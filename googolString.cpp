#include <bits/stdc++.h>
using namespace std;

void solve(long long k) {
	if (c[k] != -1) return;
	while (c[k] == -1) {
		c[current] = 0
		for (int i = current + 1; i <= 2 * current + 1; i++) {
			c[i] = 
		}
	}
}

int main() {
	cin << t;
	while (t--) {
		cin >> n;
		solve(k);
	}
}